#include "Trie2.h"
#include "Dlev.h"
#include "linkedlist.h"
//checkWord(const char* word);//check if word is a word in trie, if true return frequency else return -1.

Linked_List_Opaque_Pointer completeWord(Opaque_trie_t, char*);//find possible edits or completions

